name: build-test
on: workflow_dispatch

jobs:
  biuld:
    runs-on: ubuntu-latest

    steps:
      - name: build
        run: echo 'this is build job'

  test:
     runs-on: ubuntu-latest

     steps:
       - name: test
         run: echo 'this is test jon'
