name: printName

on: push

print:

  runs-on: ubuntu-latest

  steps:
    - name: print my name by using vari
      run: echo "helo, ${{vars.MY_NAME}}"

test:
  runs-on: ubuntu-latest

  steps:
    - name: check vari print name
      run: echo "my name is neelima"


    