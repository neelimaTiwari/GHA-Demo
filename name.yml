name: printName

on: push

jobs:

  print:
    runs-on: ubuntu-latest

    steps:
      - name: print my name by using vari
        run: echo "helo, ${{vars.MY_NAME}}"

  test:
    runs-on: ubuntu-latest

    steps:
      - name: test name is print or not
        run: echo "my name is neelima tiwari"      


    